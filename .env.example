# NomOS — Minimal Configuration
# Vault generates all other secrets automatically on first run.
NOMOS_DB_PASSWORD=your_secure_password_here

# LLM Provider (optional, can configure via Console later)
# NVIDIA_API_KEY=nvapi-...

# Runtime
NOMOS_DEV_MODE=false
NOMOS_DOMAIN=localhost
